class CreateAiCredentials < ActiveRecord::Migration[<%= ActiveRecord::Migration.current_version %>]
  def change
    create_table :ai_credentials do |t|
      t.references :owner, polymorphic: true, null: false, index: false
      t.string     :category, null: false   # "provider" or "tool"
      t.string     :name,     null: false   # e.g. "anthropic", "firecrawl"
      t.text       :api_key,  null: false   # encrypted by ActiveRecord Encryption

      t.timestamps
    end

    add_index :ai_credentials, [:owner_type, :owner_id, :category, :name],
              unique: true,
              name:   "index_ai_credentials_on_owner_and_category_and_name"
  end
end
