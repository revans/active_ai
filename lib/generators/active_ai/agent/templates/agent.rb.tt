class <%= class_name %>Agent < ApplicationAgent
  # Optional: declare tools
  # tools ActiveAI::Tools::WebSearch      # requires FIRECRAWL_API_KEY in ENV
  # tools ActiveAI::Tools::WebPageReader  # no key required

  # build_messages is inherited from ApplicationAgent — implement it there
  # if you haven't already.

  private

  # Override to give this agent its own system prompt from a file:
  #
  #   def build_system_prompt
  #     prompt_file(:<%= file_name %>)
  #   end
end
